name: build-mac-osx-x64

on: [push, pull_request]

jobs:
  build:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - name: Build
        uses: ./.github/actions/build-mac-osx
        with:
          platform: x64
    
